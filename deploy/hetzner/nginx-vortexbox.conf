# Replace vortexbox.org and www.vortexbox.org with your real domain.

upstream vortexbox_app {
    server 127.0.0.1:8080;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name vortexbox.org www.vortexbox.org;

    # Temporary: useful before SSL issuance
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        proxy_connect_timeout 60s;
        proxy_pass http://vortexbox_app;
    }

    # Upload sizes up to 1GB (matches app constraints)
    client_max_body_size 1024m;
}

